export interface Theme {
  name: string;
  icon: string;
  primaryColor: string;
  secondaryColor: string;
  background: string;
  backgroundGradient: string;
  cardBg: string;
  textColor: string;
  textSecondary: string;
  borderColor: string;
  success: string;
  warning: string;
  danger: string;
  inputBg: string;
  isDark: boolean;
}

